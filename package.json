{
  "name": "ai-driven-event-discovery-and-community-expansion",
  "version": "0.5.3",
  "description": "",
  "main": "src/webserver/dist/server.js",
  "directories": {
    "doc": "docs"
  },
  "dependencies": {
    "bowser": "^2.11.0",
    "fast-xml-parser": "^4.4.1",
    "strnum": "^1.1.2",
    "tslib": "^2.8.1",
    "tsx": "^4.20.3",
    "uuid": "^9.0.1"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "fix": "cd src/webui && ts-jest config:migrate jest.config.js",
    "start": "tsx src/webui/webserver/dist/server.js",
    "build": "cd src/webui && npm run build:server && npm run build:client",
    "build:ps": "cd src/webui && (npm run build:server:ps) && (npm run build:client:ps)",
    "build:server": "cd src/webui/webserver/ && npm install --force && tsc  --build --clean && tsc -p .",
    "build:server:ps": "(cd srv/webui/webserver/) -and (npm install --force) -and (tsc --build) -and (tsc -p .)",
    "build:client": "cd src/webui/client/ && npm install --force && npm run build",
    "build:client:ps": "(cd src/webui/client/) -and n(pm install --force) -and (npm run build)"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/evstod/AI-Driven-Event-Discovery-and-Community-Expansion.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/evstod/AI-Driven-Event-Discovery-and-Community-Expansion/issues"
  },
  "homepage": "https://github.com/evstod/AI-Driven-Event-Discovery-and-Community-Expansion#readme"
}
